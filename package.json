{
  "name": "projrest",
  "version": "1.0.0",
  "main": "main1.js",
  "scripts": {
    "build": "npx webpack",
    "deploy": "npm run build && git add dist -f && git commit -m \"Deployment commit\" && git subtree push --prefix dist origin gh-pages && git checkout main1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vlada",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "webpack": "^5.101.0",
    "webpack-cli": "^6.0.1"
  }
}
